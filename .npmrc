# Wallet adapter stack pulls @stellar/stellar-sdk, whose install scripts invoke
# `yarn setup || true` — that breaks on Windows (no yarn, and `true` is not a cmd builtin).
# Published packages still include built output; skipping scripts is safe for this app.
legacy-peer-deps=true
ignore-scripts=true
